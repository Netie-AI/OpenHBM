# Eval feedback: `refresh_mgr` -- 20260511T082200Z

Total score: **0.0 / 100**  
Threshold: 80  
Result: **FAIL**

## Stage breakdown

| stage | unit | weight | hard_fail | detail |
| --- | ---: | ---: | :---: | --- |
| lint | 0.00 | 10 |  | verible-verilog-lint not on PATH; install OSS CAD Suite |
| elab | 0.70 | 10 |  | errors=1 warnings=0 |
| sim_pass | 0.00 | 20 |  | no tests reported |
| func_cov | 1.00 | 20 |  | 4/4 bins hit (100.0%) |
| formal | 0.00 | 25 | YES | 0/1 sby tasks passed |

## What to fix next

### lint -- score 0.00

```
verible-verilog-lint not on PATH; install OSS CAD Suite
```

- Re-read `CLAUDE.md` s1.2 for banned constructs.
- Set `default_nettype none` at every file top.
- Use `logic`, never `wire`/`reg` in synthesisable code.

### elab -- score 0.70

```
errors=1 warnings=0
%Error: Invalid option: --top-module=refresh_mgr
        ... See the manual at https://verilator.org/verilator_doc.html?v=5.047 for more assistance.
```

- Verilator `--lint-only -Wall -Wpedantic --timing`.
- Most warnings come from un-driven nets, multi-driver, or width
  mismatches. Fix at the source; do not waive in the lint config.

### sim_pass -- score 0.00

```
no tests reported
```

- Make sure `dv/Makefile` honours `SIM=verilator` and
  `COCOTB_RESULTS_FILE`.
- Add a directed test for every assertion in the spec table.

### formal -- score 0.00

```
0/1 sby tasks passed
FAIL: refresh_liveness.sby (rc=1)
```

- A failing `*_bijection.sby` or `*_liveness.sby` is a HARD FAIL
  -- the merge is blocked regardless of total score.
- Read the trace under `fpv/<task>_check/engine_*/trace.vcd`.
- Strengthen the abstraction (`fpv/abs_*.sv`) before adding
  `assume property` to the DUT itself.
