SIM ?= verilator
TOPLEVEL_LANG ?= verilog
TOPLEVEL = prim_clock_buf
MODULE   = test_prim_clock_buf
VERILOG_SOURCES = $(PWD)/../rtl/prim_clock_buf.sv
include $(shell cocotb-config --makefiles)/Makefile.sim